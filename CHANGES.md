# Ragdoll Blade Arena - 修正内容

## 実装日時
2026-06-04

## 修正内容

### 1. ブラウザロード固まり問題の対策

#### main.js
- **タイムアウト検出機能を追加**：起動が8秒以上かかった場合、ユーザーに明確なエラーメッセージを表示
- **エラーハンドリングの強化**：起動失敗時に詳細なエラーメッセージを表示
- 起動成功時にタイムアウトタイマーをクリア

**効果**：
- ユーザーが白い画面で無限に待つことがなくなる
- 問題が発生した場合、ページをリロードするよう明確に指示
- ブラウザの開発者ツールでエラーログを確認可能

### 2. スティックを離さずに剣を振れる機能

#### input.js
- **フリック検出ロジックの改善**：スティック保持中に素早く動かすと剣が振れるように実装
- **ダブルスイング防止**：フリック検出後、スティックを離した時の重複スイングを防止
- **タッチ入力とマウス入力の両方に対応**

**具体的な動作**：
1. スティック/マウスを押す
2. スティックを素早く動かす（速度 > 1.1 px/ms）→ **スティック保持中に剣が振れる**
3. スティックを離す → 前回のフリックから100ms以上経過していれば、離す時の動きでも振れる

**メリット**：
- より直感的で反応性の高い操作感
- スティックを離す必要がないため、連続攻撃がしやすい
- 既存の「離すと振る」動作も保持（後方互換性）

## 技術的詳細

### タイムアウト検出の仕組み
```javascript
// 8秒以内に起動完了しなければエラー表示
let bootTimeout = setTimeout(() => {
  ui.setLoading(100, '読み込みタイムアウト');
  const lt = document.getElementById('load-text');
  if (lt) lt.textContent = '起動がタイムアウトしました。ページをリロードしてください。';
}, 8000);

// 起動成功時にクリア
clearTimeout(bootTimeout);
```

### フリック検出の改善
```javascript
// フリック検出時（速度 > 1.1 px/ms）
if (speed > 1.1 && now - (this._lastFlickFire || 0) > 220) {
  this._lastFlickFire = now;  // タイムスタンプ記録
  this.swingQueued = { dx: state.flickVec.x / m, dy: state.flickVec.y / m };
}

// リリース時：前回のフリックから100ms以上経過していれば追加スイング
if (timeSinceLastFlick > 100) {
  // スイング処理
}
```

## テスト方法

### ロード固まり問題の確認
1. ブラウザの開発者ツールを開く（F12）
2. Network タブで「Slow 3G」や「Offline」に設定
3. ページをリロード
4. 8秒以内にタイムアウトエラーが表示されることを確認

### スティック操作の確認
1. AI対戦を開始
2. スティック/マウスを押したまま、素早く動かす
3. スティックを離さずに剣が振れることを確認
4. 連続して複数回フリックできることを確認

## 互換性
- 既存の「スティック離すと振る」動作は保持
- タッチデバイスとマウス/キーボード両対応
- モバイルとデスクトップで同じ操作感

## 今後の改善案
- タイムアウト時間をデバイスに応じて調整
- ネットワーク遅延の検出と自動リトライ
- フリック感度のカスタマイズ機能
